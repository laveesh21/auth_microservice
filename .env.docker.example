# Docker Environment Variables Template
# FOR DOCKER DEVELOPMENT (docker-compose up)
# Copy to .env.docker and configure

# ===============================
# DATABASE
# ===============================
# Use host.docker.internal to connect to host machine
DB_URL=jdbc:postgresql://host.docker.internal:5432/apexauth_db
DB_USER=your_db_username
DB_PASSWORD=your_db_password

# ===============================
# JWT
# ===============================
JWT_SECRET=generate-random-secret-at-least-32-characters-long
JWT_EXPIRATION_MS=900000
JWT_REFRESH_EXPIRATION_MS=604800000

# ===============================
# SERVER
# ===============================
SERVER_PORT=8080
SPRING_PROFILE=local

# ===============================
# CORS
# ===============================
CORS_ALLOWED_ORIGINS=http://localhost:3000
